import{_ as t,o as a,c as o,an as i}from"./chunks/framework.BdQmZfLw.js";const h=JSON.parse('{"title":"Demag Internals","description":"","frontmatter":{},"headers":[],"relativePath":"dev/demag.md","filePath":"dev/demag.md","lastUpdated":null}'),r={name:"dev/demag.md"};function n(l,e,s,c,d,p){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Demag-Internals" tabindex="-1">Demag Internals <a class="header-anchor" href="#Demag-Internals" aria-label="Permalink to &quot;Demag Internals {#Demag-Internals}&quot;">​</a></h1><p>The demagnetization field is the most expensive term per step and the only one with a global (long-range) coupling, so it has its own solver family. The user level of this — how to turn periodicity on — is on the <a href="./../pbc">Periodic BC page</a>; this page records the implementation, math and verification methodology.</p><h2 id="Solver-family" tabindex="-1">Solver family <a class="header-anchor" href="#Solver-family" aria-label="Permalink to &quot;Solver family {#Solver-family}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:left;">periodicity</th><th style="text-align:left;">solver (file)</th><th style="text-align:left;">method</th></tr></thead><tbody><tr><td style="text-align:left;">open</td><td style="text-align:left;"><code>micro/demag.jl</code>, <code>micro/demag_direct.jl</code></td><td style="text-align:left;">padded-FFT convolution with Newell kernels (or direct summation, <code>fft=false</code>)</td></tr><tr><td style="text-align:left;">one axis</td><td style="text-align:left;"><code>micro/demag_pbc1d.jl</code></td><td style="text-align:left;">image sums + analytic 1D tail + DC-column fix (Lebecki 2008)</td></tr><tr><td style="text-align:left;">two axes</td><td style="text-align:left;"><code>micro/demag_pbc2d.jl</code></td><td style="text-align:left;">image sums + analytic 2D tail + DC-column fix (Wang 2010)</td></tr><tr><td style="text-align:left;">three axes</td><td style="text-align:left;"><code>micro/demag_pbc3d.jl</code></td><td style="text-align:left;">spectral projector +k̂k̂, tin-foil (Mansuripur 1989)</td></tr><tr><td style="text-align:left;">legacy</td><td style="text-align:left;"><code>micro/demag.jl</code></td><td style="text-align:left;">truncated image sums (&quot;macro&quot;), kept for compatibility</td></tr></tbody></table><p>The periodic kernel is <code>N^PBC(d) = Σ_images N(d + p·L)</code>. 1D and 2D image sums converge absolutely (1/r³ decay with dipolar cancellation), so explicit images plus a closed-form continuous tail is a strict decomposition. The 3D sum is only conditionally convergent and has no continuous tail — hence the spectral projector, under which the uniform mode carries no field.</p><h2 id="The-open/pbc1d/pbc2d-pipeline-macro-route" tabindex="-1">The open/pbc1d/pbc2d pipeline (macro route) <a class="header-anchor" href="#The-open/pbc1d/pbc2d-pipeline-macro-route" aria-label="Permalink to &quot;The open/pbc1d/pbc2d pipeline (macro route) {#The-open/pbc1d/pbc2d-pipeline-macro-route}&quot;">​</a></h2><p>pbc1d and pbc2d <strong>reuse the entire padded-FFT pipeline</strong> of the open solver (buffer allocation, FFT plan, packing, spectral multiply, in-place c2r, collect). Periodicity is implemented as three post-processing steps on the quadrant kernels: 2. <strong>Image sums</strong> — the tensor-building kernels take non-zero image counts (<code>Ic</code>, <code>Jc</code>);</p><ol start="3"><li><p><strong>Per-entry analytic tail</strong> — a KernelAbstractions kernel adds the closed-form continuous-tail contribution (1D: Lebecki&#39;s continuous approximation <code>T_ab(x0, v, w)</code>, error ~Xc⁻³; 2D: Wang 2010 Eq. 10–15 inclusion–exclusion <code>Ninf</code>, error ~γ^(−3/2));</p></li><li><p><strong>DC-column fix</strong> — the uniform-mode response is replaced by its analytic value (1D: rod identity, exactly zero for axial uniform states; 2D: zero for in-plane uniform states, exact self-term +1 for open-axis diagonal components).</p></li></ol><p>Why padded FFT gives periodicity for free: the circular wrap of the padded convolution implements the image sum exactly on the periodic axes (the kernel is already periodic), while zero-padded magnetization degrades open axes to the linear (open) response. The wrap never touches the Nyquist plane, so zeroing it is harmless.</p><p>pbc3d is deliberately separate: the projector is direction-independent, initialisation is free (no Newell evaluation) and each step is ~2× faster than the macro route.</p><h2 id="Sign-conventions-classic-trap" tabindex="-1">Sign conventions (classic trap) <a class="header-anchor" href="#Sign-conventions-classic-trap" aria-label="Permalink to &quot;Sign conventions (classic trap) {#Sign-conventions-classic-trap}&quot;">​</a></h2><p>The package kernel is the <strong>negative</strong> of the field kernel: <code>H = -Σ N·M</code>, so far-field <code>N_xx(u, 0, 0) &lt; 0</code>. Self-derived tail formulas written in the field-kernel convention (positive <code>(2u²−ρ²)/r⁵</code> structure) are off by a global sign. The published <code>Ninf</code> (Wang 2010) and <code>N^cont</code> (Lebecki 2008) are both already in the package convention — no negation. Cross-checking magnitudes but not signs has burned a full debugging cycle here once; compare signs too.</p><p>Two more traps from implementation history, both caught by brute-force baselines:</p><ul><li><p><strong>Permutation tables</strong>: kernel components are built in a canonical frame and mapped back (<code>PBC1D_PCOMP</code> / <code>PBC2D_PCOMP</code>). A transcription error left axial components correct while lateral tensor DC columns got garbage — random textures looked fine, so cross-checks must cover every axis/direction pair.</p></li><li><p><strong>Test-state components</strong>: <code>m[3*(axis-1)+1:3:end]</code> starts at the wrong offset for axis=2 (sets x, not y). The lateral-uniform test state has a <em>legitimate</em> nonzero response, so a &quot;wrong but physical-looking&quot; signal passes casually. Use <code>m[axis:3:end]</code>.</p></li></ul><h2 id="Verification-methodology" tabindex="-1">Verification methodology <a class="header-anchor" href="#Verification-methodology" aria-label="Permalink to &quot;Verification methodology {#Verification-methodology}&quot;">​</a></h2><p>The pattern that works, in order: 2. <strong>Brute-force image-sum baseline first</strong>: direct summation with the same sign convention, explicit images over all periodic directions. Self-check the baseline itself: macro-N ≡ brute-force-N at 1e-11 proves the baseline before any comparison.</p><ol start="3"><li><p><strong>Per-buffer pipeline diff</strong>: <code>m_pad → M_pad → M_out → h_pad</code> compared stage by stage against a hand-written Julia pipeline; the first divergent buffer localises the bug.</p></li><li><p><strong>Zero-response probes</strong>: states that must produce exactly zero field (1D axial uniform, 2D in-plane uniform) are the most sensitive detectors — any error shows up in full. Caveat: they are &quot;small difference of large numbers&quot;, so they do not represent general accuracy, and the test state itself must be verified first.</p></li><li><p><strong>Analytic special cases</strong> from Lebecki App. B.7 and Wang Eq. 13 as independent anchors.</p></li></ol><h2 id="Accuracy-and-cost-F64/CPU,-measured" tabindex="-1">Accuracy and cost (F64/CPU, measured) <a class="header-anchor" href="#Accuracy-and-cost-F64/CPU,-measured" aria-label="Permalink to &quot;Accuracy and cost (F64/CPU, measured) {#Accuracy-and-cost-F64/CPU,-measured}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:left;">item</th><th style="text-align:left;">value</th></tr></thead><tbody><tr><td style="text-align:left;">pbc1d field error vs brute force</td><td style="text-align:left;">Ic=2 → 6×10⁻⁵, Ic=4 → 2.5×10⁻⁶</td></tr><tr><td style="text-align:left;">pbc2d field error</td><td style="text-align:left;">Ic=2 → ~1×10⁻⁴</td></tr><tr><td style="text-align:left;">uniform-state (amplified) error</td><td style="text-align:left;">pbc1d Ic=2 → 2.2×10⁻⁴·Ms, Ic=6 → ≈0</td></tr><tr><td style="text-align:left;">init (kernel construction)</td><td style="text-align:left;">open 0.07 s, pbc1d 0.57 s (DC fix 0.25 s), pbc2d 0.4–2.4 s (grows with layers), pbc3d ≈0</td></tr><tr><td style="text-align:left;">per step</td><td style="text-align:left;">pbc3d ~2× faster than macro; pbc1d/pbc2d (macro route) = open-route speed</td></tr></tbody></table><p>The FFT plan is cached and reused across steps. GPU demag currently relies on the cuFFT/default-stream sharing assumption; porting to other GPU backends needs that assumption re-checked (see <a href="./performance">Performance</a>).</p><p>The neighbor-loop terms (exchange, DMI) have their own acceleration layer — see <a href="./params">Parameter System</a> for the class/pair-table stencil partition.</p>',21)])])}const g=t(r,[["render",n]]);export{h as __pageData,g as default};
