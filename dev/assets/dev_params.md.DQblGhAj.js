import{_ as a,o as t,c as o,an as i}from"./chunks/framework.gM3-hPsp.js";const m=JSON.parse('{"title":"Parameter System: Fill and make_param","description":"","frontmatter":{},"headers":[],"relativePath":"dev/params.md","filePath":"dev/params.md","lastUpdated":null}'),s={name:"dev/params.md"};function r(n,e,l,c,d,p){return t(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="Parameter-System:-Fill-and-make_param" tabindex="-1">Parameter System: Fill and make_param <a class="header-anchor" href="#Parameter-System:-Fill-and-make_param" aria-label="Permalink to &quot;Parameter System: Fill and make_param {#Parameter-System:-Fill-and-make_param}&quot;">​</a></h1><p>Every user-facing parameter — <code>Ms</code>, <code>alpha</code>, <code>Ku</code>, <code>D</code>, <code>temperature</code>, <code>pins</code>, exchange and DMI constants — accepts a <code>NumberOrArrayOrFunction</code>. This page documents how that union is materialised internally, and the invariants the implementation relies on.</p><h2 id="Design-rule" tabindex="-1">Design rule <a class="header-anchor" href="#Design-rule" aria-label="Permalink to &quot;Design rule {#Design-rule}&quot;">​</a></h2><blockquote><p><strong>Spatiality is data, not type.</strong> A parameter is either a <code>Fill</code> (O(1) representation of a uniform array) or a dense array. &quot;Uniform&quot; is one legal array representation, not a separate type. No <code>Spatial*</code> twin types are introduced anywhere.</p></blockquote><p>Before the Fill refactor the type system encoded uniform vs spatial (<code>UniformExchange</code> vs <code>SpatialExchange</code>, <code>LLG</code> vs <code>SpatialLLG</code>, ...), which duplicated kernels, structs and call sites, and materialised O(N) storage for every uniform parameter. The single remaining discrimination point is the setter moment: <code>set_alpha(sim, 0.1)</code> stores <code>Fill(0.1, n)</code>, while a function or array is materialised into a dense array. After construction the type is fixed — no per-step dispatch.</p><h2 id="The-Fill-type" tabindex="-1">The Fill type <a class="header-anchor" href="#The-Fill-type" aria-label="Permalink to &quot;The Fill type {#The-Fill-type}&quot;">​</a></h2><p><code>src/fill.jl</code> vendors a ~40-line <code>Fill{T,N} &lt;: AbstractArray{T,N}</code> instead of depending on FillArrays.jl, because kernel-argument behaviour must be controlled exactly (see the <code>@Const</code> rules below):</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Fill{T,N} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AbstractArray{T,N}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">T</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    size</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NTuple{N,Int}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>Properties the implementation relies on:</p><ul><li><p><strong>isbits when <code>T</code> is isbits</strong> — a <code>Fill{Float32,1}</code> kernel argument is passed by value, <code>f[i]</code> compiles to the same machine code as a scalar, and saves a device load;</p></li><li><p>O(1) <code>sum</code> (<code>value * prod(size)</code>), used by <code>average_m</code>;</p></li><li><p><strong>immutable</strong> — <code>copyto!(fill, ...)</code>, <code>.=</code>, <code>.*=</code> on a Fill throw; all write sites must target dense arrays;</p></li><li><p><code>Fill{AbstractFloat,1}</code> is <em>not</em> isbits (abstract element type): the symbolic eigen mode must not pass such Fills to kernels expecting concrete data.</p></li></ul><h2 id="make_param:-the-single-materialisation-point" tabindex="-1">make_param: the single materialisation point <a class="header-anchor" href="#make_param:-the-single-materialisation-point" aria-label="Permalink to &quot;make_param: the single materialisation point {#make_param:-the-single-materialisation-point}&quot;">​</a></h2><p>All setters route through <code>make_param(T, v, mesh, n_total; scale=1)</code> (<code>src/params.jl</code>):</p><ul><li><p><code>v isa Number</code> (and <code>T</code> isbits) → <code>Fill(T(v), n_total)</code>;</p></li><li><p><code>v isa Function</code> or array → materialised on the <strong>host</strong> and pushed to the device with a single <code>copyto!</code> (GPU arrays cannot broadcast host arrays — never sample into a device array directly);</p></li><li><p>eltype is always exactly <code>T</code> (the precision frozen at <code>Sim</code> construction).</p></li></ul><p><code>make_vector_param</code> is the 3-component analogue for vector parameters (<code>axis</code>, DMI directions, ...), returning three dense component arrays.</p><h2 id="Kernel-contracts" tabindex="-1">Kernel contracts <a class="header-anchor" href="#Kernel-contracts" aria-label="Permalink to &quot;Kernel contracts {#Kernel-contracts}&quot;">​</a></h2><ol start="2"><li><p><strong><code>@Const</code> annotations</strong>: only parameters that can never be a <code>Fill</code> keep <code>@Const</code> (spin, field, neighbor tables, ...). Anything that may be passed as a Fill (<code>alpha</code>, <code>mu0_Ms</code>, <code>Ku</code>, <code>D</code>, <code>temperature</code>, <code>pins</code>, ...) must NOT be annotated — <code>@Const</code> on an isbits struct has unverified behaviour across backends. When adding a kernel, follow this split.</p></li><li><p><strong>No FP64 literals in kernels</strong>: <code>1.0 / x::Float32</code> promotes to Float64 arithmetic, and GeForce-class GPUs run FP64 at 1/32 throughput. Write <code>T(1.0)</code>, <code>one(T)</code>, or precompute <code>inv(T(v))</code> host-side (the stencil layer&#39;s <code>inv_ms</code>). This was a measured +38% step-time regression once; the full cleanup landed in <code>fb6ead9c</code>.</p></li><li><p><strong>Property assignment semantics</strong>: <code>sim.driver.alpha = 0.1</code> still works via a <code>setproperty!</code> hook on <code>LLG</code> — the scalar is converted in place to a same-length <code>Fill</code>, arrays get their eltype matched, and functions throw (spatial sampling needs the mesh; use <code>set_alpha</code>).</p></li></ol><h2 id="Stencil-partition-layer-exchange-/-DMI" tabindex="-1">Stencil partition layer (exchange / DMI) <a class="header-anchor" href="#Stencil-partition-layer-exchange-/-DMI" aria-label="Permalink to &quot;Stencil partition layer (exchange / DMI) {#Stencil-partition-layer-exchange-/-DMI}&quot;">​</a></h2><p>Neighbor-loop terms get an extra fast path when parameters are piecewise-constant per region (<code>src/micro/stencil.jl</code>):</p><ul><li><p><code>sim.mat_class[I] = mu0_Ms[I] &gt; 0 ? compressed_region_id : 0</code> — class 0 means vacuum and absorbs all masking (a class-0 row/column in every table is zero); region 0 (background) is <strong>not</strong> class 0: non-vacuum cells on the background region are a legal material class.</p></li><li><p>Per term and axis, an <code>(R+1)²</code> pair table <code>A_eff(class_I, class_J)</code> is precomputed <strong>iff every cell of a class shares the same parameter value</strong>. If the check passes, the partition kernel runs with one byte-class gather and no division or parameter gather in the inner loop; otherwise the inline kernel runs unchanged (semantic fallback).</p></li><li><p><code>_MAX_CLASSES = 16</code>: more regions degrade to a 0/1 vacuum mask and the inline path (keeps the table in L1).</p></li><li><p>Invalidation: <code>FDMesh.layout_version</code> is bumped by both <code>set_region</code> methods (checked lazily per term); <code>set_Ms</code> drops the caches eagerly.</p></li><li><p>Limitation: partitioning only applies to <strong>static</strong> DMI (<code>ft === _static_time</code>); time-modulated DMI always takes the inline kernel.</p></li></ul><h2 id="Symbolic-mode" tabindex="-1">Symbolic mode <a class="header-anchor" href="#Symbolic-mode" aria-label="Permalink to &quot;Symbolic mode {#Symbolic-mode}&quot;">​</a></h2><p><code>set_precision(AbstractFloat)</code> lets parameters carry <code>FlatTerm</code> symbolic values for the eigenmode workflow. In this mode <code>Fill</code> of parameters falls back to dense materialisation where isbits-ness is required, and the eigen code paths keep their <code>Vector{AbstractFloat}</code> buffers. Do not &quot;simplify&quot; these to concrete element types.</p>`,21)])])}const u=a(s,[["render",r]]);export{m as __pageData,u as default};
