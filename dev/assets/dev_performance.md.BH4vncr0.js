import{_ as t,o as a,c as o,an as n}from"./chunks/framework.gM3-hPsp.js";const p=JSON.parse('{"title":"Performance Notes","description":"","frontmatter":{},"headers":[],"relativePath":"dev/performance.md","filePath":"dev/performance.md","lastUpdated":null}'),r={name:"dev/performance.md"};function s(i,e,c,l,d,h){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="Performance-Notes" tabindex="-1">Performance Notes <a class="header-anchor" href="#Performance-Notes" aria-label="Permalink to &quot;Performance Notes {#Performance-Notes}&quot;">​</a></h1><p>Working notes for making things fast without breaking things. The full issue-by-issue tracker lives in the repository root (<code>PERFORMANCE.md</code>); this page holds the stable knowledge.</p><h2 id="Measure-first" tabindex="-1">Measure first <a class="header-anchor" href="#Measure-first" aria-label="Permalink to &quot;Measure first {#Measure-first}&quot;">​</a></h2><ul><li><p>Timing is built in via TimerOutputs: <code>MicroMagnetic.reset_timer()</code> before a run, then <code>println(MicroMagnetic.timer)</code> afterwards. <code>run_step</code>, <code>run_until</code>, and per-term field evaluation already carry <code>@timeit</code> sections.</p></li><li><p><strong>GPU timings are not wall-clock truth</strong>: explicit synchronisation was removed from the hot path, so per-section times on GPU backends are lower bounds with unknown overlap. Use profiling tools (<code>nsys</code>, <code>ncu</code> on CUDA) for real numbers.</p></li><li><p>Discipline for any optimization: record a baseline (the mumax3 standard problems, a 3D FFT-heavy case, a 2D skyrmion case, NEB) → change one thing → re-measure.</p></li></ul><h2 id="Kernel-rules-invariants,-not-suggestions" tabindex="-1">Kernel rules (invariants, not suggestions) <a class="header-anchor" href="#Kernel-rules-invariants,-not-suggestions" aria-label="Permalink to &quot;Kernel rules (invariants, not suggestions) {#Kernel-rules-invariants,-not-suggestions}&quot;">​</a></h2><ol start="2"><li><p><strong>No FP64 literals in Float32 kernels.</strong> <code>1.0 / x::Float32</code> promotes the whole expression to Float64; on GeForce FP64 throughput is 1/32 of FP32, so a handful of promoted tail operations cost as much as the entire neighbor loop. Write <code>T(1.0)</code> / <code>one(T)</code> / precomputed <code>inv(T(v))</code>. The 25 residual sites were fixed in <code>fb6ead9c</code>; keep new kernels clean.</p></li><li><p><strong><code>@Const</code> only on never-Fill arguments</strong> (see <a href="/MicroMagnetic.jl/dev/dev/params#kernel-contracts">Parameter System</a>).</p></li><li><p><strong>Derive the kernel backend from data, never from the global</strong> (<code>KernelAbstractions.get_backend(sim.spin)</code>), and never from host-side temporaries — a CPU kernel compiled against device arrays fails with scalar indexing.</p></li><li><p>Uniform parameters arrive as <code>Fill</code> and are <strong>isbits pass-by-value</strong>: do not &quot;unpack&quot; them into arrays for convenience.</p></li></ol><h2 id="Known-structure-of-the-cost" tabindex="-1">Known structure of the cost <a class="header-anchor" href="#Known-structure-of-the-cost" aria-label="Permalink to &quot;Known structure of the cost {#Known-structure-of-the-cost}&quot;">​</a></h2><ul><li><p><strong>Per-term launch + accumulate</strong>: <code>effective_field</code> launches one kernel per interaction and then a <code>vector_add</code> per term. Many-terms simulations pay launch overhead; merged-reduction and exch+DMI loop fusion (they share the neighbor table; DMI&#39;s 4 neighbors are a subset of exchange&#39;s 6) are the next planned wins, designed as launcher-internal fusions so each term keeps its separate energy output.</p></li><li><p><strong>Kernel objects are re-instantiated each step</strong> (closure over backend + groupsize); KA caches compilations but instantiation is not free. Caching per <code>(backend, groupsize)</code> is a candidate once profiling shows it matters.</p></li><li><p><strong>Demag FFT</strong>: plans are cached and reused. On CUDA, the implementation assumes cuFFT work on the default stream interacts correctly with KA kernels; other GPU backends must re-validate this assumption before trusting timings.</p></li><li><p><strong>Stencil partition layer</strong> (<code>micro/stencil.jl</code>): class/pair tables remove divisions and parameter gathers from the exchange/DMI inner loops. Reference numbers from the implementation branch: 256² CPU/Float64 inline 569 μs → partitioned 499 μs (vs 440 μs for the old pre-Fill scalar kernel); on GPU, launch overhead dominates at 256² (~20 μs) and 1024² dense cases were ~1.5× faster. Only static DMI partitions; the inline kernel is the semantic fallback everywhere else.</p></li><li><p><strong>Stopping criterion</strong>: <code>relax</code> computes the full <code>dm/dt</code> maximum every step just for convergence checking; down-sampling is a possible future change (it would alter stop timing, so it needs a documented default change).</p></li><li><p><strong>FSAL</strong>: DOPRI54 currently discards its 7th stage, which mathematically equals the next step&#39;s <code>k1</code> — enabling FSAL is a −14% step-time, identity transformation (validate against the Butcher table first).</p></li></ul><h2 id="Saving-and-I/O" tabindex="-1">Saving and I/O <a class="header-anchor" href="#Saving-and-I/O" aria-label="Permalink to &quot;Saving and I/O {#Saving-and-I/O}&quot;">​</a></h2><p><code>save_ovf</code> / <code>save_vtk</code> must copy device arrays to the host (<code>Array(...)</code>) — unavoidable, but avoid doing it twice for the same data on hot paths (e.g. when one step both stores a snapshot and contributes to an energy column).</p>',10)])])}const m=t(r,[["render",s]]);export{p as __pageData,m as default};
